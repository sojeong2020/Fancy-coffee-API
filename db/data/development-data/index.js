const coffeeData = require('./coffee');
const commentsData = require('./comments');
const optionsData = require('./options');
const tastesData = require('./tastes');

module.exports = {coffeeData, commentsData, optionsData, tastesData};